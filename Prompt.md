شما یک توسعه‌دهنده ارشد Full-Stack، متخصص UI/UX و مهندس DevOps هستید.
من یک نمونه اولیه (Prototype) از اپلیکیشن مدیریت مزرعه در فایل `farm-windows.html` دارم. می‌خواهم با حفظ منطق و هدف اصلی این نمونه اولیه، آن را به یک سیستم کاملاً مدرن، فوق‌العاده زیبا، خلاقانه و مقیاس‌پذیر تبدیل کنید.

لطفاً مراحل زیر را به صورت گام‌به‌گام در محیط کار من (Workspace) اجرا کنید. پس از پایان هر گام، بدون تایید من به گام بعدی بروید:

### گام اول: بازطراحی UI/UX و ارتقای کدهای فرانت‌اند (مدرن و Mobile-First)
۱. ظاهر اپلیکیشن را کاملاً بازطراحی کنید. ساختار می‌تواند تغییر کند اما باید نوآورانه و مطابق با ترندهای روز طراحی رابط کاربری (مثل Glassmorphism ملایم، سایه‌های نرم، و رنگ‌های زنده) باشد. همه تصاویر تغییر کند و در هر سربرگ، تصویر مرتبط با خودش در بالای صفحه ظاهر شود
۲. طراحی باید ۱۰۰٪ واکنش‌گرا (Responsive) باشد. تجربه کاربری در موبایل باید خیره‌کننده باشد و وابسته به اینکه کاربر فونت دارد یا ندار دنباشد؛ دکمه‌ها، فرم‌ها و آیتم‌ها باید بزرگ (Touch-friendly)، خوش‌چین و ارگونومیک باشند تا کاربر در محیط مزرعه به راحتی با آن کار کند.
۳. بخش‌های مختلف (حضور و غیاب، آبیاری و...) را در کارت‌های مجزا با الگوهای پس‌زمینه (Patterns)، رنگ‌های جذاب و تصاویر/آیکون‌های مرتبط و زیبا طراحی کنید.
۴. امکانات جدید بخش «حضور و غیاب»:
   - در جدول لیست حضور و غیاب، علاوه بر دکمه «حذف»، دکمه «ویرایش» را اضافه کنید تا اطلاعات مجدداً به فرم برگردد و قابل تغییر باشد.
   - فیلدهای حضور و غیاب را به دو بخش جداگانه «شیفت صبح» و «شیفت عصر» تفکیک کنید.
   - وضعیت «مرخصی» (با قابلیت انتخاب با حقوق/بدون حقوق) را به گزینه‌ها اضافه کنید.
۵. دکمه «طراح»: یک دکمه بسیار ریز، ظریف و زیبا (مثلاً در فوتر یا گوشه صفحه) با عنوان «طراح» قرار دهید. با کلیک روی آن، یک صفحه یا مودال (Modal) بسیار شیک و مدرن باز شود که شامل تصویر من، رزومه، تخصص‌ها و راه‌های ارتباطی باشد.

### گام دوم: بک‌اند و تضمین قطعی عدم از دست رفتن داده‌ها (Zero Data Loss)
۱. یک بک‌اند اختصاصی (ترجیحاً Next.js App Router یا Node.js) با دیتابیس PostgreSQL (توسط Prisma یا Drizzle) در یک پوشه مجزا ایجاد کنید.
۲. منطق سینک کردن (Sync) را به این API جدید متصل کنید. 
۳. معماری Offline-First (با Dexie.js) را حفظ و تقویت کنید. سیستم باید طوری باشد که با قطعی اینترنت هیچ دیتایی از بین نرود و در صورت وصل شدن شبکه، دیتا بدون تکراری شدن (Idempotent با استفاده از شناسه یکتا) به سرور منتقل شود.
۴. یک داشبورد مجزا، ایمن و بسیار جذاب برای «مالک مزرعه» بسازید تا دیتای روزانه را در قالب نمودارهای زیبا و مدرن مشاهده کند.

### گام سوم: مستندسازی مدرن و تعاملی
۱. از مدرن‌ترین ابزار مستندسازی جاوااسکریپت (مانند Nextra یا Docusaurus) استفاده کنید.
۲. یک سایت مستندات (Docs) بسیار زیبا، روان و تعاملی بسازید که شامل معرفی اپلیکیشن، معماری سیستم و راهنمای کاربری باشد (فقط نسخه وب، نیازی به PDF نیست).

### گام چهارم: کنترل نسخه و دیپلوی در GitHub
۱. این پروژه باید دقیقاً در ریپازیتوری زیر قرار گیرد:
   https://github.com/sabernaseralavi-60/2026_Farm-Management-App
۲. مخزن گیت (Git) را در روت پروژه Initialize کنید و فایل `.gitignore` استاندارد بسازید.
۳. با استفاده از توکن زیر، ریموت را تنظیم کرده و تمام کدها را در شاخه اصلی Push کنید:
   توکن: [حذف‌شده از مخزن عمومی — به‌جای آن از اعتبار موجود gh CLI استفاده شد]
   (دستورات git remote set-url و git push را با این توکن اجرا کن).
